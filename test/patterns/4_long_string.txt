{'long' : "this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing. this is a very long string that will be repeated many times to test if the library handles large buffers correctly in error messages and general parsing.\n"}
